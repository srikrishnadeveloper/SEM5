# GROK MASTER — R2 NOTEBOOK GATE
Date: 2026-09-06
Notebook SHA (reported): 59e5ec8d934de6e8d9623414dd4d5b4fa01120dbf6bac6d76327586f378e3949
Inspected: embedded `4_sweep_inference.py`, `3_infer_cascade.py`, `config.py`, `geometry.py`, `pq.py`

## VERDICT: GO (fast path only)

Do **not** retrain. Attach the 0.350 kernel and sweep those weights.

## Answers

### Q1 — Attach `notebooka464bcfe13` vs fresh retrain?

**Attach. Fast path is mandatory, not optional.**

Retrain = new seed ≠ the 0.350 checkpoint. Then the val table cannot explain the 0.350 LB number.

Human setup (exactly these four):
1. GPU: Tesla T4 x2
2. Internet: ON
3. **Add data**: competition `filament-segmentation-2026`
4. **Add input → Your Work**: completed kernel `notebooka464bcfe13` (the 0.350 run). Attach **nothing else** that contains another `best.pt`

If Cell 6/7 print “Existing … weights detected” and skip train, you are on the right path.
If they start YOLO epoch 1, **stop the kernel**. Weights were not found.

Expected wall time: **~15–25 min** (Stage A ~5–8 on 128 val disks + Stage B CPU sweep + Stage C 180 test). The 5–8 min claim is optimistic. Still fine.

### Q2 — Freeze `overlap_mode=allow` if SQ is higher?

**No.** Freeze the row with max **pq_mean**, not max SQ.

Tie-break (apply when reading the table, no code change):
- If `best.pq_mean - baseline.pq_mean < 0.005`, **keep 0.350 settings** (`conf=0.25, min_area=100, fallback=0, trim`). Do not overfit an 80-point grid on 128 disks.
- If the gap is ≥ 0.005, freeze the winner including `allow` if that is the winner.

## What I confirmed is correct

- Stage A caches YOLO+refiner once at `conf=0.10`, Stage B is CPU-only. This is the right design.
- Square crop via `geometry.square_bounds_from_bbox` is in both train and infer.
- Empty disks emit 0 rows.
- Categories 1–4 included in converter.
- Kirillov match is `IoU > 0.5`.
- Dual-GPU placement YOLO `cuda:0` / refiner `cuda:1`.
- Argmax `pq_mean` is piped into Stage C.

## What to ignore in the table

- **TP/FP/FN are summed across annotators** (double-counted). Do not interpret them as instance counts. Use `n_pred`, `pq_mean`, `sq_mean`, `rq_mean`.
- **GT mean/disk** is per-annotator observation, not unique-disk union. Compare chosen preds/disk to that mean, not to 11.4.

## Silent failure to watch (not a NO-GO)

YOLO-mask fallback does:

```python
yolo_masks_raw[b_idx].astype(np.uint8)
```

If `masks.data` is float in `[0,1]`, that **zeros the mask**. Then every `fallback=1` row will match its `fallback=0` twin (`n_pred` identical). That does **not** mean YOLO masks are useless; it means the fallback experiment never ran.

After the table prints:
- If fallback=1 ≡ fallback=0 for all rows → note it in the paste, still submit the pq_mean winner.
- If fallback=1 actually changes `n_pred` and wins by ≥ 0.005 → keep it.

Do not rebuild the notebook for this. Rebuild is R3.

## After the kernel finishes, paste to Grok

1. The full `VAL_PQ_TABLE`
2. `CHOSEN_OPERATING_POINT` block
3. Whether Cell 6/7 skipped training
4. Test CSV: `n_rows`, mean/p50/p90 rows/image, empty stems
5. Whether fallback=1 rows differed from fallback=0

Do **not** treat a new LB number as success until that paste.

## Forbidden in this run

- YOLO11m
- Residual U-Net
- Extra datasets / extra `best.pt`
- “I cancelled and retrained because 8 minutes became 20”
