# GROK MASTER — R3 GO
Notebook SHA claimed: 29a7120d69e83bd2b7bf678e3236c23f6aa1725bc0e64575804d152a15ea61ed
Inspected embedded `v8_1/3_infer_cascade.py` from the attached notebook.

## VERDICT: GO

The host-reject path is closed.

Confirmed in the notebook that will run:
- Cell 8 calls `3_infer_cascade.py --conf 0.20 --min-area 400 --yolo-fallback 0 --overlap-mode trim --tta`
- `run_cascade_inference` raises if `overlap_mode != trim`
- After trim arbitration it always runs `sanitize_instances_zero_overlap` and asserts pairwise overlap == 0
- Cell 9 re-decodes the CSV and asserts the same per disk
- Cells 6/7 still skip train when `notebooka464bcfe13` is attached
- Cell 8 does **not** call `4_sweep_inference.py`

`4_sweep_inference.py` is still embedded in Cell 4. Harmless as long as nobody runs it. Do not run it.

## Kaggle attach (same as last good run)

- GPU T4 x2
- Internet ON
- Data: `filament-segmentation-2026`
- Input: `notebooka464bcfe13` only

Stop if Cell 6/7 print `Epoch 1/20`.

## What to expect

- Runtime 4–8 min if weights attach; ~45 min if they do not (that is a mistake)
- Rows likely **~900–1400**, not the illegal 1354
- Empty disks allowed
- Public LB should be near **0.35**, maybe 0.35–0.38. It will not be 0.46. That number was `allow`.

## After the kernel

Paste:
1. Train skip yes/no
2. `OVERLAP ASSERTION PASS` line
3. n_rows, mean/p50, empty stems
4. Public LB if it scores
